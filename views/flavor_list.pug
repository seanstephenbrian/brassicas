extends layout 

block content 
    h1= title 

    ul 
        each flavor in flavor_list
            li 
                a(href=flavor.url) #{flavor.flavor}
        else 
            li There are no flavors.